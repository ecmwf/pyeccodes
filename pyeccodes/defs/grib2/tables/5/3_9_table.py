def load(h):
    return ({'bit': 1, 'on': 0, 'title': 'Clockwise orientation'},
            {'bit': 1,
             'on': 1,
             'title': 'Anti-clockwise (i.e., counter-clockwise) orientation'})
